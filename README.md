# Harness Harlot landing page

The public Astro site for [Harness Harlot](https://github.com/highlyproteus/harness-harlot), a native terminal workstation for macOS and Linux.

## Local development

```sh
npm install
npm run dev
```

Build the static site with `npm run build`.
